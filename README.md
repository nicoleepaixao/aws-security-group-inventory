# aws-security-group-inventory
A multi-account, multi-region Security Group inventory tool for AWS. Generates an Excel report identifying default security groups, SSH exposure, public access, and resource attachments across all scanned accounts.
